<?xml version="1.0" encoding="utf-8"?>
<!-- Native splash shown between process start and Flutter's first frame.
     Matches the app's cream background + logo so it flows straight into
     the _StartupSplash screen in main.dart instead of a plain white flash. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/splash_background" />
    <item>
        <bitmap
            android:gravity="center"
            android:src="@mipmap/ic_launcher" />
    </item>
</layer-list>
