import 'package:flutter/material.dart';

/// GaonHaat brand palette — "mandi/haat" theme.
/// Kept as a single source of truth so every screen stays visually consistent.
class AppColors {
  AppColors._();

  static const Color green = Color(0xFF1F5F3F);
  static const Color greenDark = Color(0xFF164A31);
  static const Color mustard = Color(0xFFE8A33D);
  static const Color cream = Color(0xFFFBF7F0);
  static const Color charcoal = Color(0xFF26241F);
  static const Color sage = Color(0xFFEDF3EA);
  static const Color line = Color(0xFFE1D9C8);
  static const Color muted = Color(0xFF8A8471);
  static const Color mutedDark = Color(0xFF6B6656);
  static const Color inactive = Color(0xFFD8D2C0);
}
